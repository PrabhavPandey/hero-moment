# hero moment

find the best 30-45 seconds from any interview.

## run locally

```bash
brew install ffmpeg  # or apt-get install ffmpeg
pip install -r requirements.txt
streamlit run main.py
```

## deploy

push to github → connect to [streamlit cloud](https://streamlit.io/cloud) → add `GEMINI_API_KEY` to secrets.
